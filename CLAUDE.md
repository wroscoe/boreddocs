# Claude instructions for this repo

- **Don't deploy or push unless explicitly asked.** Make local edits and commits freely; stop short of `git push`, tag pushes, `gh run`, or anything that triggers the docs/release workflows or publishes to PyPI.
